#include<stdio.h>
int main()
{
    int  a,b,c,sum;
    printf("enter the angle of triangle\n");
    scanf("%d%d%d",&a,&b,&c);
    sum = a+b+c;
    if (sum==180)
    printf("triangle is valid");
    else
    printf("triangle is not valid");
    return 0;
}
